package com.kaampay.entity;

public class EmployeeEnums {

    // Gender Enum
    public enum Gender {
        Male, Female, Other
    }

    // Salary Type Enum
    public enum SalaryType {
        Monthly, Hourly, Daily
    }

    // User Role Enum
    public enum UserRole {
        Employee, AttendanceManager, Admin, Owner
    }
}